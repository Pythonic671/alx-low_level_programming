#include "main.h"
/**
 * add - adds two integers and return the results
 * @a: int to be be added to b
 * @b: int to be added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
